/** Darkness added when the wizard touches a gloom mite (0–1 scale). */
export const GLOOM_MITE_DARKNESS_SPIKE = 0.08;

/** Horizontal patrol speed in pixels per second. */
export const GLOOM_MITE_PATROL_SPEED = 80;

/** On-screen gloom mite sprite width/height. */
export const GLOOM_MITE_DISPLAY_SIZE = 32;

/** Minimum ms between gloom mite hits on the player. */
export const GLOOM_MITE_HIT_COOLDOWN_MS = 1200;

/** Horizontal knockback applied to the wizard on hit. */
export const GLOOM_MITE_KNOCKBACK_X = 180;

/** Platform runs shorter than this do not spawn a gloom mite. */
export const MIN_GLOOM_MITE_RUN_LENGTH = 4;
